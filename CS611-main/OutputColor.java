/*
his enum is used to highlight what colors
the terminal can show for an output, hence
the name "OutputColor"
 */
public enum OutputColor {
    RED,
    GREEN,
    YELLOW,
    BLUE,
    PURPLE,
    CYAN,
    WHITE
}
